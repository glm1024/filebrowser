# 产品业务档案

## 项目定位

本项目是基于开源 File Browser 的二开版本，用于在指定目录内提供 Web 文件管理能力，支持文件上传、下载、预览、编辑、删除和分享等基础文件操作。

## 核心角色

- 系统管理员：负责服务部署、全局配置、用户管理、权限设置和文件空间管理。
- 普通用户：在授权目录内进行日常文件上传、下载、查看和整理。

## 主要流程

- 部署人员启动服务并指定文件根目录、配置目录和数据库位置。
- 管理员初始化或维护用户账号与权限。
- 用户登录 Web 界面后，在授权范围内管理文件。
- 服务使用本地单文件数据库保存配置、用户和权限等信息，不依赖 MySQL。

## 关键规则

- 文件访问范围受启动时指定的根目录约束。
- 本地源码启动默认使用 `.local-dev/files` 作为文件根目录，`.local-dev/filebrowser.db` 作为开发数据库。
- Docker 部署文档中保留了二开镜像和默认账号说明；本地源码开发优先使用脚本启动，不依赖 Docker。
- 普通访客访问根地址时默认以 `ftp` 身份进入文件区，不需要手动填写 `ftp/ftp`；管理员通过“管理员登录”入口输入密码进入管理态。
- 匿名访客身份直接使用内置 `ftp` 账号的实际权限；权限边界由本地或生产环境中的 `ftp` 账号配置决定。

## 当前约束

- 项目后端为 Go，前端为 Vue，生产构建需要先生成前端静态资源再编译后端二进制。
- 当前 fork 仓库没有 Git tag，构建出的版本号可能只显示提交号，不影响本地运行。

## 产品决策

### 访客默认访问与管理员登录分离

为减少普通用户访问门槛，系统采用服务端访客身份方案：未登录用户访问文件区时，后端读取内置 `ftp` 用户并按该用户的实际权限签发访客会话；前端不展示或提交 `ftp/ftp` 密码，避免把共享账号密码暴露到页面逻辑里。

管理员入口独立保留。访客态侧边栏显示“管理员登录”，点击后进入登录页并输入管理员密码。管理员退出后回到访客文件区，而不是停留在登录页。

该方案放弃直接使用 File Browser 官方 `noauth`，原因是 `noauth` 会把所有访问都视为同一个默认用户，无法同时保留 `sysadmin` 管理员登录；也不依赖反向代理注入身份，避免本地部署时额外配置代理和 header 信任边界。
